"""FastAPI routers for neo4j_retrieval_service.

This package is intentionally a real Python package (has `__init__.py`) to avoid
namespace-package collisions with other microservices that also expose a top-level
`routers` module.
"""

