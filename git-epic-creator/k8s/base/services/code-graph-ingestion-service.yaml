apiVersion: apps/v1
kind: Deployment
metadata:
  name: code-graph-ingestion-service
  namespace: agentic-ai
  labels:
    app: code-graph-ingestion-service
    component: ingestion
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: code-graph-ingestion-service
  template:
    metadata:
      labels:
        app: code-graph-ingestion-service
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: agentic-ai-workload-identity
      containers:
        - name: code-graph-ingestion-service
          image: code-graph-ingestion-service:latest
          ports:
            - containerPort: 8015
              name: http
          envFrom:
            - configMapRef:
                name: agentic-ai-config
          env:
            - name: API_PORT
              value: "8015"
          volumeMounts:
            - name: secrets-store
              mountPath: "/mnt/secrets-store"
              readOnly: true
            - name: tmp
              mountPath: /tmp
          livenessProbe:
            httpGet:
              path: /health
              port: 8015
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8015
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
      volumes:
        - name: secrets-store
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: agentic-ai-secrets
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: code-graph-ingestion-service
  namespace: agentic-ai
spec:
  selector:
    app: code-graph-ingestion-service
  ports:
    - port: 80
      targetPort: 8015
      protocol: TCP
  type: ClusterIP


