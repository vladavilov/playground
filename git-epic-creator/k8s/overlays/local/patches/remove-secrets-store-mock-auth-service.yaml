apiVersion: apps/v1
kind: Deployment
metadata:
  name: mock-auth-service
  namespace: agentic-ai
spec:
  template:
    spec:
      containers:
        - name: mock-auth-service
          volumeMounts:
            - name: secrets-store
              $patch: delete
      volumes:
        - name: secrets-store
          $patch: delete


