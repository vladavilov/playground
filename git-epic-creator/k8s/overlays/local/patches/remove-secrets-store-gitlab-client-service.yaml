apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitlab-client-service
  namespace: agentic-ai
spec:
  template:
    spec:
      containers:
        - name: gitlab-client-service
          volumeMounts:
            - name: secrets-store
              $patch: delete
      volumes:
        - name: secrets-store
          $patch: delete


