apiVersion: apps/v1
kind: Deployment
metadata:
  name: neo4j-ingestion-service
  namespace: agentic-ai
spec:
  template:
    spec:
      containers:
        - name: neo4j-ingestion-service
          volumeMounts:
            - name: secrets-store
              $patch: delete
      volumes:
        - name: secrets-store
          $patch: delete


