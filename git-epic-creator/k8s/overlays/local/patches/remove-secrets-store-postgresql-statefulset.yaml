apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgresql
  namespace: agentic-ai
spec:
  template:
    spec:
      containers:
        - name: postgresql
          volumeMounts:
            - name: secrets-store
              $patch: delete
      volumes:
        - name: secrets-store
          $patch: delete


