apiVersion: apps/v1
kind: Deployment
metadata:
  name: neo4j-repository-service
  namespace: agentic-ai
spec:
  template:
    spec:
      containers:
        - name: neo4j-repository-service
          volumeMounts:
            - name: secrets-store
              $patch: delete
      volumes:
        - name: secrets-store
          $patch: delete


