apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-init-service
  namespace: agentic-ai
spec:
  template:
    spec:
      containers:
        - name: db-init-service
          volumeMounts:
            - name: secrets-store
              $patch: delete
      volumes:
        - name: secrets-store
          $patch: delete


