apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: neo4j
  namespace: agentic-ai
spec:
  template:
    spec:
      containers:
        - name: neo4j
          volumeMounts:
            - name: secrets-store
              $patch: delete
      volumes:
        - name: secrets-store
          $patch: delete


