apiVersion: apps/v1
kind: Deployment
metadata:
  name: project-management-service
  namespace: agentic-ai
spec:
  template:
    spec:
      containers:
        - name: project-management-service
          volumeMounts:
            - name: secrets-store
              $patch: delete
      volumes:
        - name: secrets-store
          $patch: delete


