apiVersion: apps/v1
kind: Deployment
metadata:
  name: sse-bridge-service
  namespace: agentic-ai
spec:
  template:
    spec:
      containers:
        - name: sse-bridge-service
          volumeMounts:
            - name: secrets-store
              $patch: delete
      volumes:
        - name: secrets-store
          $patch: delete


