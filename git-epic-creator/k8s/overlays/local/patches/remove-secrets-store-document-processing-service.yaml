apiVersion: apps/v1
kind: Deployment
metadata:
  name: document-processing-service
  namespace: agentic-ai
spec:
  template:
    spec:
      containers:
        - name: document-processing-service
          volumeMounts:
            - name: secrets-store
              $patch: delete
      volumes:
        - name: secrets-store
          $patch: delete


