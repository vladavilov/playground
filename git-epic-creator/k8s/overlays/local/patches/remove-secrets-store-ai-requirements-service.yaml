apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-requirements-service
  namespace: agentic-ai
spec:
  template:
    spec:
      containers:
        - name: ai-requirements-service
          volumeMounts:
            - name: secrets-store
              $patch: delete
      volumes:
        - name: secrets-store
          $patch: delete


