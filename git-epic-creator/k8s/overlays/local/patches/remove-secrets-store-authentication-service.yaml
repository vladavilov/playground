apiVersion: apps/v1
kind: Deployment
metadata:
  name: authentication-service
  namespace: agentic-ai
spec:
  template:
    spec:
      containers:
        - name: authentication-service
          volumeMounts:
            - name: secrets-store
              $patch: delete
      volumes:
        - name: secrets-store
          $patch: delete


